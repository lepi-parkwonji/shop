/// 관리자
model Admin {
  id          Int      @id @default(1)
  usrname     String   @unique
  password    String
  displayName String   @default("관리자")
  faqs        Faq[]
  notices     Notice[]
}

enum AdminRole {
  // 관리자
}
