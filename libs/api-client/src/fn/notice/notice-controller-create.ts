/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateNoticeDto } from '../../models/create-notice-dto';
import { NoticeResponseDto } from '../../models/notice-response-dto';

export interface NoticeControllerCreate$Params {
      body: CreateNoticeDto
}

export function noticeControllerCreate(http: HttpClient, rootUrl: string, params: NoticeControllerCreate$Params, context?: HttpContext): Observable<StrictHttpResponse<NoticeResponseDto>> {
  const rb = new RequestBuilder(rootUrl, noticeControllerCreate.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<NoticeResponseDto>;
    })
  );
}

noticeControllerCreate.PATH = '/api/notice';
