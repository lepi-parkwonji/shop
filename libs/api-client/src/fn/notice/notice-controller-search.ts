/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { NoticeResponseDto } from '../../models/notice-response-dto';
import { PageInfoDto } from '../../models/page-info-dto';

export interface NoticeControllerSearch$Params {
  pageNo?: number;
  pageSize?: number;
  query?: string;
}

export function noticeControllerSearch(http: HttpClient, rootUrl: string, params?: NoticeControllerSearch$Params, context?: HttpContext): Observable<StrictHttpResponse<{
'items': Array<NoticeResponseDto>;
'pageInfo': PageInfoDto;
}>> {
  const rb = new RequestBuilder(rootUrl, noticeControllerSearch.PATH, 'get');
  if (params) {
    rb.query('pageNo', params.pageNo, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('query', params.query, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      'items': Array<NoticeResponseDto>;
      'pageInfo': PageInfoDto;
      }>;
    })
  );
}

noticeControllerSearch.PATH = '/api/notice/search';
