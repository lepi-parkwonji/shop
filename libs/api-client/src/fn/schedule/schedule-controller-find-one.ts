/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ScheduleResponseDto } from '../../models/schedule-response-dto';

export interface ScheduleControllerFindOne$Params {
  id: number;
}

export function scheduleControllerFindOne(http: HttpClient, rootUrl: string, params: ScheduleControllerFindOne$Params, context?: HttpContext): Observable<StrictHttpResponse<ScheduleResponseDto>> {
  const rb = new RequestBuilder(rootUrl, scheduleControllerFindOne.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ScheduleResponseDto>;
    })
  );
}

scheduleControllerFindOne.PATH = '/api/schedule/{id}';
